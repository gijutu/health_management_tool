<%= form_with(model: better_condition, local: true) do |form| %>
  <% if better_condition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(better_condition.errors.count, "error") %> prohibited this better_condition from being saved:</h2>

      <ul>
      <% better_condition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
